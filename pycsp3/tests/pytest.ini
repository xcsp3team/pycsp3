[pytest]
python_files = test_*.py
addopts = -ra --strict-markers
xfail_strict = true
timeout = 120
markers =
    solving: the test solves models with ACE, CHOCO and COSOCO (set automatically on the tests using the fixture solver)
